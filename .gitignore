node_modules/
dist/
*.log
.DS_Store
.frites/
coverage/
*.tsbuildinfo

# Environment files — never commit secrets
.env
.env.*
!.env.example

# Machine-local Claude Code config (git identity enforcement, etc.) — never commit
.claude/settings.local.json
